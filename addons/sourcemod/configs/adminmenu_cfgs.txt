/**
 * List config files here (relative to moddir) to have them added to the exec config menu list
 * Left side is the filename, right side is the text to be added to the menu
 */
Configs
{
	"cfg/ffc_config/closeacs.cfg"			"Disable ACS"
	"cfg/ffc_config/vocal_unlimit.cfg"			"Vocalize Unlimited"
	"cfg/ffc_config/vocal_mute.cfg"			"Vocalize Mute"
	"cfg/ffc_config/vocal_limit.cfg"			"Vocalize Standard"
}
